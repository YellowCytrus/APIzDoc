# Бэкенд

 - [x] Добавить слой репозитория: перетащить логику доступа к данным из роутеров в реп
 - [ ] Добавить проверку размера загружаемых файлов. Проверять не только расширение файла, но и MIME‑тип и по возможности его сигнатуру. Не доверять имени файла от клиента: генерим своё (UUID + нужное расширение), не допускать спецсимволы. Хранить файлы в каталоге с правами 500 (r-x------) или 555
 - [ ] Сокрыть детали внутренних ошибок, чтобы они не уходили в HTTP 500
 - [ ] Добавить rate limiting
 - [ ] Добавить OAuth 2.0
 - [ ] Добавить MFA/2FA
 - [ ] Добавить JWT (HttpOnly cookie)
 - [ ] Добавить воркеры для будущей работы с s3
 - [ ] Устранить потенциальный path traversal в file.filename
 - [ ] app/api/elements/factory.py (17): ElementDescriptor[Any, Any, Any] : Убрать Any вместо дженериков, такая же тема в дескрипторах
 - [ ] Не понимаю что происходит в app/utils/typst_preamble.py
 - [ ] В app/api/upload.py стр.24 зачем то разрешены gif

# Превью

- [ ] Убедиться что превью и конечный pdf док не расходятся в оформлении
- [ ] Кривое неудобное масштабирование: 1 - всё скачет (хочется увеличиваться аккурат в точку куда навёдён курсор мыши); 2 - при сильном увеличении слишком рано упираюсь в попытке пролистать горизонтально влево, и чуть чуть аналогично вправо
- [ ] Листая вниз до упора добавить дополнительное пространство для возможности пролистать ниже
- [ ] В превью не отображаются переопределения (как минимум для par)

# Фичи

 - [ ] Импорт/Экспорт настроек профиля стилей
 - [ ] Версионирование документа
 - [ ] Подсветка ошибок + подсказки к ним
 - [ ] Добавить опции для абзаца: first-line-indent и amount (в api и в frontend)
 - [ ] Висячие отступы для списков
 - [ ] Эффект для DragOver в frontend/src/components/MdEditor.vue
 - [ ] Смена темы md-editor-v3 (нативно много всяких настроек есть) через gui


# Технические

- [ ] Отступ первой строки у элемента "Абзац" должен быть в миллиметрах, а не как сейчас в em. Подобные проблемы в: отступ в списках (в pt, надо в мм), все параметры в абзаце,

# Баги

- [x] Ошибки при настройке нумерации заголовков
- [x] Первый абзац не имеет отступа первой строки, хотя должен
- [ ] Переопределение стилей для списков: дла нумерованых списков переопределение срабатывает только на номер элемента, а содержимое списка, если это обычный текст - не переопределяется. При этом я попробовал создать маркирвоаный список в качестве элемента нумерованного списка - вот к нему переопределение применилось, что кстати тоже не корректно. Короче говоря надо сделать кнопку "наследовать стиль от родителя" чтобы вложенные элементы могли наследовать или не наследовать стили


# Временные решения (не устранять пока статус не "убрать")

- [ ] Статус "не трогать": generate.py, Editor.vue - кастомный титульник который можно выбрать для генерации
